 Server XAMPP

// Path
 C: c:/xampp/htdocs

// Create project
 composer create-project laravel/laravel iot-device-laravel-project

// Run project
 php artisan serve


// Create database Mysql
CREATE DATABASE iot;

// Make model
php artisan make:model nombre_modelo -m

// Navigate to Model
app\Models\nombre_modelo.php

// Migrate
php artisan migrate


// Install dependencies
composer install

// Copy .env
cp .env.example .env

// Generate key
php artisan key:generate

// Run project
php artisan serve

// Clear cache
php artisan optimize:clear

// Create model station
php artisan make:model Station -m

// Create models
php artisan make:model Country -m
php artisan make:model Department -m
php artisan make:model City -m
php artisan make:model Station -m
php artisan make:model User -m
php artisan make:model Sensor -m
php artisan make:model SensorData -m


// Create controller
php artisan make:controller DashboardController


// Resources
index = lista
create = crear
store = guardar
show = mostrar
edit = editar
update = actualizar
destroy = eliminar

// Create controller
php artisan make:controller StationController --resource --model=Station

php artisan make:controller SensorController --resource --model=Sensor

php artisan make:controller DataApiController --resource --model=DataApi

// Migrates replace
php artisan migrate:fresh

// Clear config
php artisan config:clear
